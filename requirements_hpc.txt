# Core scientific
numpy>=1.26
pandas>=2.0
scipy>=1.11

# HuggingFace stack
torch>=2.3.0
transformers>=4.51.0     # Gemma 4 (gemma4 model type) requires 4.51+
accelerate>=0.30.0       # device_map="auto" multi-GPU / CPU offload
bitsandbytes>=0.43.0     # 4-bit quantisation (USE_4BIT=True)

# Optional: Flash Attention 2 for faster A100 inference
# pip install flash-attn --no-build-isolation
